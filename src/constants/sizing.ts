/**
 * Image sizing constants for consistent sizing throughout the app
 */

/**
 * Standard size for all images (header images, tray items, sorted items)
 * All images will be rendered at this size to maintain visual consistency
 */
export const STANDARD_IMAGE_SIZE = 100; // pixels

/**
 * Scale factor applied to items during drag operations
 * Dragged items are scaled to 110% of the standard size
 */
export const DRAG_SCALE_FACTOR = 1.1;
